name: docs-validation

on:
  pull_request:
  push:

jobs:
  docs-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install docs-validation dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ripgrep

      - name: Run docs validation
        run: bash scripts/docs-validation/run.sh
